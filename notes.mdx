```bash
docker run -it ubuntu
```

Explanation:
This command tells Docker to run a new container using the Ubuntu image (a lightweight Ubuntu OS).

-i → Keeps STDIN open (interactive mode)

-t → Allocates a terminal so you can interact with the container directly